name: OCR Workflow

on: [push, pull_request]
jobs:
  ocr:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:mantic
    steps:
      - uses: actions/checkout@main
      - name: Set up Python
        uses: actions/setup-python@main
        with:
          python-version: "3.12"
      - name: Install Dependencies
        run: |
          apt-get update && apt-get install -y --no-install-recommends tesseract-ocr which && \
          python3 -m pip install --upgrade pytesseract requests pdf2image reportlab
      - name: Tesseract Commands 
        run: tesseract -v && tesseract --list-langs
      - name: Run Code
        run: python3 ocr.py

        
